# Excel Agent Configuration

# LLM Settings
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=your-api-key-here
LLM_BASE_URL=

# MCP Server (excel-mcp-server)
# Default: uses venv python to run the script directly (auto-detected)
# Override only if you need a custom setup:
# MCP_SERVER_COMMAND=/path/to/python
# MCP_SERVER_ARGS=["/path/to/read_file_server.py"]

# App
DEBUG=false
